{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "phases": {
        "build": {
          "dependsOn": ["install"],
          "cmds": ["npm run build"]
        }
      }
    }
  },
  "deploy": {
    "startCommand": "caddy run --config /Caddyfile --adapter caddyfile"
  }
}

